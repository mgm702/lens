version: 2

project_name: lens

before:
  hooks:
    - go mod tidy

builds:
  - id: lens
    main: ./cmd/lens
    binary: lens
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: lens
    name_template: "lens_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE
      - examples/**

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: mgm702
    name: lens
  draft: false
  prerelease: auto
