# Example: Generic (Starter Template)

A minimal template to copy when building a new experiment from scratch.
All fields are documented inline in `experiment.yaml`.

## Run it

```bash
export ANTHROPIC_API_KEY=...
lens run examples/generic/experiment.yaml
```

## Or use the scaffold command

```bash
lens new my-eval
```

This generates the same structure pre-filled with your experiment name.
